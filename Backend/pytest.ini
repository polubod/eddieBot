[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning

# Run with coverage: pytest --cov=app --cov-report=term-missing --cov-fail-under=95
